package util

import models.{ProductPath, PurchasedProduct, PurchasedProductInstance}

object ProductTreeUtils {

  //TODO: Finish implementing
//  def buildOtherGoodsPurchasedProducts(path: String, quantity: Int): List[PurchasedProduct] = {
//    val purchasedProductInstances: List[PurchasedProductInstance] = {
//
//      for(i <- 0 to quantity) yield PurchasedProductInstance(ProductPath(path), s"iid$i", None, None, Some("USD"), Some(BigDecimal(i * 20)))
//    }.toList
//    List(PurchasedProduct(ProductPath(path), Some(quantity), purchasedProductInstances))
//  }

}
